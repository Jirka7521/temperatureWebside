// Template configuration used inside the docker image.
// At container start `/docker-entrypoint.sh` replaces placeholders with
// actual environment values and writes `config.js` into the web root.
// Do not edit this file at runtime; set environment variables instead.
//
// Placeholders below are intentionally simple `${VAR}` tokens so they
// are compatible with `envsubst` and `sed` used by the entrypoint script.
// The generated `config.js` will contain concrete values and be loaded by
// the frontend as `window.inlineConfig`.
//
// Example override: `docker run -e POSITION_LAT=50.0755 -e POSITION_LON=14.4378 ...`
//
// NOTE: INDOOR_API_ADDRESS is replaced with the computed backend address
// by the entrypoint script to allow automatic detection in local networks.
//
// Auto-generated from environment variables at container start
window.inlineConfig = {
  temperatureThresholds: {
    cold:   { value: ${TEMP_THRESHOLD_COLD_VALUE}, color: "${TEMP_THRESHOLD_COLD_COLOR}" },
    cool:   { value: ${TEMP_THRESHOLD_COOL_VALUE}, color: "${TEMP_THRESHOLD_COOL_COLOR}" },
    normal: { value: ${TEMP_THRESHOLD_NORMAL_VALUE}, color: "${TEMP_THRESHOLD_NORMAL_COLOR}" },
    warm:   { value: ${TEMP_THRESHOLD_WARM_VALUE}, color: "${TEMP_THRESHOLD_WARM_COLOR}" }
  },

  // Public weather API used for outdoor forecasts (forecast API)
  apiAddress: "${OUTDOOR_API_ADDRESS}",
  // Public archive API used for historical data (Open-Meteo archive)
  archiveApiAddress: "${OUTDOOR_ARCHIVE_API_ADDRESS}",
  indoorApiAddress: "${INDOOR_API_ADDRESS}",
  indoorApiEndpointCurrent: "${INDOOR_API_ENDPOINT_CURRENT}",
  indoorApiEndpointRange: "${INDOOR_API_ENDPOINT_RANGE}",

  indoorApiParams: {
    start: "${INDOOR_API_PARAM_START}",
    end: "${INDOOR_API_PARAM_END}",
    interval: "${INDOOR_API_PARAM_INTERVAL}"
  },

  indoorDataMaxAge: ${INDOOR_DATA_MAX_AGE},
  position: { latitude: ${POSITION_LAT}, longitude: ${POSITION_LON} },
  refreshInterval: ${REFRESH_INTERVAL_MS}
};
